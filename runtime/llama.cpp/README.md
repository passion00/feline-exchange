# Managed llama.cpp runtime

Feline installs its pinned, verified `llama-server` distribution under `install/`. Runtime binaries, archives, partial downloads, and extraction staging are ignored by Git. Use `python3 -m feline ai install` rather than placing unverified executables here.
